# ドキュメントレビュー (2026-03-21)

CLAUDE.md、コード内コメント (doxygen)、Sphinx ドキュメント (docs/)、
ヘッダー宣言とソース実装の整合性を包括的にレビューした結果を以下に示す。

KNOWN_ISSUES.md に記載されている項目は除外済み。

---

## 1. CLAUDE.md — ファイル構造セクションの不備

- [x] `include/bigint.hpp` が File structure セクションに記載されていない。`bdd_types.h`, `bdd_ops.h`, `bdd.h` から include されている主要ヘッダーである。
- [x] `src/pidd.cpp` が File structure セクションに記載されていない。CMakeLists.txt の `KYOTODD_LIB_SOURCES` に含まれており、PiDD の実装ファイルである。
- [x] `src/rotpidd.cpp` が File structure セクションに記載されていない。CMakeLists.txt の `KYOTODD_LIB_SOURCES` に含まれており、RotPiDD の実装ファイルである。
- [x] `src/seqbdd.cpp` が File structure セクションに記載されていない。CMakeLists.txt の `KYOTODD_LIB_SOURCES` に含まれており、SeqBDD の実装ファイルである。
- [x] `test/test_pidd.cpp` が File structure セクションに記載されていない。CMakeLists.txt でビルドされるテストファイルである。
- [x] `test/test_rotpidd.cpp` が File structure セクションに記載されていない。CMakeLists.txt でビルドされるテストファイルである。
- [x] `test/test_seqbdd.cpp` が File structure セクションに記載されていない。CMakeLists.txt でビルドされるテストファイルである。
- [x] `test/test_ddbase.cpp` が File structure セクションに記載されていない。CMakeLists.txt でビルドされるテストファイルである。
- [x] `python/` ディレクトリ (Python バインディング) が File structure セクションに記載されていない。`python/src/kyotodd/_binding.cpp` に pybind11 による全クラスのバインディングが存在する。
- [x] `docs/` ディレクトリ (Sphinx ドキュメント) が File structure セクションに記載されていない。`docs/api/`, `docs/tutorials/`, `docs/concepts.rst` 等の Sphinx ドキュメントが存在する。
- [x] `app/` ディレクトリ (サンプルアプリケーション) が File structure セクションに記載されていない。`app/BDDQueen/` に BDD/ZDD の N-Queens 実装例が存在する。

## 2. CLAUDE.md — DDBase クラスセクションの不備

- [x] `DDBase::new_var(int n, bool reverse)` (複数変数一括作成版) が DDBase セクションに記載されていない。`include/dd_base.h:90-98` で定義されている。
- [x] `DDBase` のコンストラクタにおける `bddnull` の扱い (負の整数で `bddnull`) が CLAUDE.md に記載されていない。`include/dd_base.h:39-43` の `explicit DDBase(int val)` では `val < 0` で `bddnull` を設定する。

## 3. CLAUDE.md — BDD/ZDD クラスの主要機能の未記載

- [x] `BDD::prime(bddvar v)` / `BDD::prime_not(bddvar v)` (正/負リテラル生成) が CLAUDE.md に記載されていない。`include/bdd_types.h:510-513`。
- [x] `BDD::cube()` / `BDD::clause()` (DIMACS 形式の連言/選言生成) が CLAUDE.md に記載されていない。`include/bdd_types.h:515-518`。
- [x] `BDD::count(bddvar n)` / `BDD::exact_count(bddvar n)` (BDD の充足割当カウント) が CLAUDE.md に記載されていない。BDD 固有の機能として `include/bdd_types.h:421-448`。
- [x] `BDD::uniform_sample()` / `ZDD::uniform_sample()` (一様サンプリング) が CLAUDE.md に記載されていない。`include/bdd_types.h:462-463`, `include/bdd_types.h:739-740`。
- [x] `ZDD::enumerate()` (全集合の列挙) が CLAUDE.md に記載されていない。`include/bdd_types.h:760`。
- [x] `ZDD::from_sets()` (集合リストからの ZDD 構築) が CLAUDE.md に記載されていない。`include/bdd_types.h:1020`。
- [x] `ZDD::power_set()` (べき集合) が CLAUDE.md に記載されていない。`include/bdd_types.h:1016-1018`。
- [x] `ZDD::combination(n, k)` (k 要素部分集合) が CLAUDE.md に記載されていない。`include/bdd_types.h:1022`。
- [x] `ZDD::singleton(v)` / `ZDD::single_set(vars)` (シングルトン/単一集合生成) が CLAUDE.md に記載されていない。`include/bdd_types.h:1011-1013`。
- [x] `ZDD::has_empty()` / `bddhasempty()` (空集合メンバーシップチェック) が CLAUDE.md に記載されていない。`include/bdd_types.h:1004`, `include/bdd_ops.h:557`。
- [x] `ZDD::random_family()` (テンプレート版一様ランダムファミリー生成) が CLAUDE.md に記載されていない。`include/bdd_types.h:1034-1035`。`ZDD_Random()` のみ記載されている。
- [x] `ZddCountMemo` / `BddCountMemo` クラスが CLAUDE.md に記載されていない。`include/bdd_types.h:163-203`。exact_count のメモ化テーブルを管理するクラスである。
- [x] `ZDD::print_sets()` / `ZDD::to_str()` (集合ファミリーの文字列表現) が CLAUDE.md に記載されていない。`include/bdd_types.h:892-934`。
- [x] `BDD::cache_get()` / `BDD::cache_put()` / `ZDD::cache_get()` / `ZDD::cache_put()` / `QDD::cache_get()` / `QDD::cache_put()` (型付きキャッシュ API) が CLAUDE.md に記載されていない。

## 4. CLAUDE.md — GC セクションの未記載 API

- [x] `bddfinal()` (ライブラリ終了処理) が CLAUDE.md に記載されていない。`include/bdd_base.h:37` で宣言されている。全リソースを解放する関数。
- [x] `bddgc_rootcount()` (登録済み GC ルート数の取得) が CLAUDE.md に記載されていない。`include/bdd_base.h:372`。
- [x] `bdd_check_reduced()` (ノードの reduced フラグ検証) が CLAUDE.md に記載されていない。`include/bdd_base.h:385`。

## 5. CLAUDE.md — BDD/ZDD child アクセサの不完全な記載

- [x] CLAUDE.md の DDBase セクションでは `raw_child0()`, `raw_child1()`, `raw_child()` のみ記載しているが、BDD/ZDD/QDD 各クラスには complement エッジを解決する `child0()`, `child1()`, `child()` メソッドも存在する。BDD は lo/hi 両方を flip、ZDD は lo のみ flip、QDD は lo/hi 両方を flip する。

## 6. CLAUDE.md — I/O 関連の未記載

- [x] `bddplainsize()` (complement 展開後のノード数) が CLAUDE.md に記載されていない。`include/bdd_base.h:167-191`。BDD/ZDD クラスの `plain_size()` メソッドとして公開。
- [x] `bddrawsize()` (複数 DAG の共有ノード数) が CLAUDE.md に記載されていない。`include/bdd_base.h:178`。
- [x] Sapporo フォーマット I/O (`bdd_export_sapporo`, `bdd_import_sapporo`, `zdd_export_sapporo`, `zdd_import_sapporo`) が CLAUDE.md に記載されていない。
- [x] Graphillion フォーマット I/O (`zdd_export_graphillion`, `zdd_import_graphillion`) が CLAUDE.md に記載されていない。
- [x] Graphviz DOT 出力 (`bdd_save_graphviz`, `zdd_save_graphviz`, `GraphvizMode` enum) が CLAUDE.md に記載されていない。
- [x] Binary フォーマット multi-root I/O (`bdd_export_binary_multi`, `zdd_export_binary_multi`, `qdd_export_binary_multi` 等) が CLAUDE.md に記載されていない。

## 7. コード内コメント (doxygen) の問題

- [x] `bddlit()` の doxygen コメント (`include/bdd_ops.h:527-528`) が存在しない関数 `bddexactlit()` を参照している。「Use bddexactlit() for exact results beyond this limit.」と記載されているが、`bddexactlit` は宣言も実装も存在しない。正しい代替は `bddexactcount()` を使って正確なカウントを行い、lit 情報は別途計算する方法となる。

## 8. Sphinx ドキュメント (docs/) の問題

- [x] QDD クラスの C++ API ドキュメント (`docs/api/cpp/ops_qdd.rst` 相当) が存在しない。`docs/api/cpp/index.rst` に QDD のエントリがない。
- [x] UnreducedDD クラスの C++ API ドキュメントが存在しない。`docs/api/cpp/index.rst` に UnreducedDD のエントリがない。
- [x] QDD クラスの Python API ドキュメント (`docs/api/python/qdd.rst`) が存在しない。`docs/api/python/index.rst` に QDD のエントリがない。Python バインディングは `python/src/kyotodd/_binding.cpp` に存在する。
- [x] UnreducedDD クラスの Python API ドキュメント (`docs/api/python/unreduced_dd.rst`) が存在しない。Python バインディングは `python/src/kyotodd/_binding.cpp` に存在する。
- [x] `docs/api/cpp/types.rst` に QDD と UnreducedDD の `doxygenclass` ディレクティブがない。BDD と ZDD のみドキュメント化されている。

## 9. CLAUDE.md — SeqBDD セクションの不完全な記載

- [x] SeqBDD の concatenation (`operator*`, SeqBDD の連結操作) が key operations に記載されていない。`include/seqbdd.h:26`。
- [x] SeqBDD の left quotient (`operator/`, 左商) が key operations に記載されていない。`include/seqbdd.h:27`。
- [x] SeqBDD の remainder (`operator%`, 左剰余) が key operations に記載されていない。`include/seqbdd.h:160`。
- [x] SeqBDD の `print_seq()` / `seq_str()` / `export_to()` が CLAUDE.md に記載されていない。

## 10. CLAUDE.md — RotPiDD セクションの不完全な記載

- [x] RotPiDD の `Swap(a, b)` (転置操作) が key operations に記載されていない。`include/rotpidd.h:201`。PiDD と同じ操作が RotPiDD にも存在する。
- [x] RotPiDD の `Cofact(u, v)` (cofactoring) が key operations に記載されていない。`include/rotpidd.h:219`。
- [x] RotPiDD の `Odd()` / `Even()` (奇/偶置換の抽出) が key operations に記載されていない。`include/rotpidd.h:224-229`。
- [x] RotPiDD の `RotBound(n)` が key operations に記載されていない。`include/rotpidd.h:235`。
- [x] RotPiDD の `RemoveMax(k)` / `normalizeRotPiDD(k)` が CLAUDE.md に記載されていない。`include/rotpidd.h:263-273`。
- [x] RotPiDD の `VECtoRotPiDD()` / `RotPiDDToVectorOfPerms()` / `normalizePerm()` (ベクトル-RotPiDD 変換) が CLAUDE.md に記載されていない。`include/rotpidd.h:282-296`。
- [x] RotPiDD の `contradictionMaximization()` (重み付き矛盾最大化) が CLAUDE.md に記載されていない。`include/rotpidd.h:343-348`。

## 11. CLAUDE.md — PiDD セクションの不完全な記載

- [x] PiDD の `operator*` (置換の合成) / `operator/` (除算) が CLAUDE.md に記載されていない。`include/pidd.h:94-95`。
- [x] PiDD の `Print()` / `Enum()` / `Enum2()` (列挙・表示操作) が CLAUDE.md に記載されていない。`include/pidd.h:215-219`。

## 12. CLAUDE.md — BDD 操作関数の未記載

- [x] `bddsmooth()` (単一変数の存在量化、`bddexistvar` と同等) が CLAUDE.md に記載されていない。`include/bdd_ops.h:212`。
- [x] `bddspread()` (変数値の近隣レベルへの拡散) が CLAUDE.md に記載されていない。`include/bdd_ops.h:220`。
- [x] `bddnand()` / `bddnor()` / `bddxnor()` (複合論理演算) が CLAUDE.md に記載されていない。`include/bdd_ops.h:48-64`。
- [x] `bddsymdiff()` (ZDD 対称差) が CLAUDE.md に記載されていない。`include/bdd_ops.h:314`。
- [x] `bddremainder()` (ZDD 剰余) が CLAUDE.md に記載されていない。`include/bdd_ops.h:359`。
- [x] `bddjointjoin()` (ZDD ジョイントジョイン) が CLAUDE.md に記載されていない。`include/bdd_ops.h:383`。
- [x] `bdddisjoin()` (ZDD ディスジョイントプロダクト) が CLAUDE.md に記載されていない。`include/bdd_ops.h:371`。

## 13. CLAUDE.md — `_rec` 分離パターンセクションのメンテナンス

- [x] `_rec` 分離パターンセクションで `bddcard/bddlit/bddlen/bddcount_rec/bddexactcount_rec` と列挙されているが、これらはいずれも `BDD_RecurGuard` を使用する方式に移行済み (`src/zdd_adv.cpp` 確認済み)。`bdd_gc_guard` テンプレートの説明は引き続き正確であるが、`_rec` 関数名の具体例が現状の実装と一致しているか確認が必要。

## 14. CLAUDE.md — BDD_RecurGuard / BDD_RecurLimit の未記載

- [x] `BDD_RecurGuard` (再帰深度ガード RAII) が CLAUDE.md に記載されていない。`include/bdd_internal.h:27-35`。`BDD_RecurLimit` (デフォルト 8192) で再帰深度を制限する。
- [x] `BDD_RecurCount` (現在の再帰深度カウンター) が CLAUDE.md に記載されていない。`include/bdd_types.h:55`。

## 15. CLAUDE.md — キャッシュ構造の未記載

- [x] 3-operand キャッシュ API (`bddrcache3`, `bddwcache3`) が CLAUDE.md に記載されていない。`include/bdd_base.h:318-320`。ITE 操作で使用される。
- [x] キャッシュエントリ構造体 `BddCacheEntry` のフィールドレイアウト (`fop`, `g`, `h`, `result`) が CLAUDE.md に記載されていない。`include/bdd_types.h:78-83`。

## 16. CLAUDE.md — BDD/ZDD/QDD の変換 API の不完全な記載

- [x] `BDD::to_qdd()` と `ZDD::to_qdd()` は記載されているが、`QDD::to_bdd()` と `QDD::to_zdd()` の具体的な実装方法 (jump rule / zero-suppression の適用方向) の説明が不足している。

---

## 統計

| カテゴリ | 件数 |
|---------|------|
| CLAUDE.md ファイル構造の不備 | 11 |
| CLAUDE.md クラス/API の未記載 | 36 |
| コード内コメントの問題 | 1 |
| Sphinx ドキュメントの欠落 | 5 |
| **合計** | **53** |
